<h1>Carrito de Compras</h1>
{{#if cart.products.length}}
  <ul>
    {{#each cart.products}}
      <li>
        {{this.productId.title}} - Cantidad: {{this.quantity}}
      </li>
    {{/each}}
  </ul>
  <a href="/checkout">Realizar Pago</a>
{{else}}
  <p>El carrito está vacío</p>
{{/if}}
